# Dependências do multi-tarefa na Kaggle, pinadas no que roda local.
#
# `torch` NÃO está aqui de propósito: a Kaggle já vem com um build de torch casado
# com o CUDA da imagem, e deixar o pip trocá-lo por um build genérico é o jeito mais
# rápido de perder a GPU. O mjlab exige `torch>=2.7.0`, e as imagens atuais da Kaggle
# satisfazem — a célula de verificação confere isso e falha alto se não.
#
# O mjlab declara o resto das dependências (incluindo `rsl-rl-lib==5.4.0` exato), então
# instalar ele puxa a árvore inteira. Os pins abaixo são o que roda local, para a
# sessão da Kaggle não pegar uma versão diferente sem você saber.
mjlab==1.5.1
mujoco==3.10.0
mujoco-warp==3.10.0.2
warp-lang==1.15.0
rsl-rl-lib==5.4.0
tensordict==0.13.0
tyro==1.0.15
tensorboard==2.21.0
scipy>=1.15
numpy<2.5
