# ACTIVE = config que a task REGISTRADA usa (treino sem override E play.py).
# Aponta pro que está sendo trabalhado AGORA.
# Cadeia: gate_and_com -> box_edge -> generalize (pos variada + push sob carga).
# table_low1 (mesa baixada) fica pronto pra quando for rodar o currículo de altura.
from .c2026_07_16_generalize import KNOBS as ACTIVE
